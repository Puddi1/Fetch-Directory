# Recursive C file fetcher

The script fetches all files on your current directory and subdirectories, besides the bin and .git folder, and prints them.

To run it:

```bash
make
```

To check memory leaks:

```bash
make meml
```